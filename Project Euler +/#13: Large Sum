def first_ten_digits_sum(numbers):
    total_sum = sum(numbers)
    return str(total_sum)[:10]

# Input
N = int(input())
numbers = []

for _ in range(N):
    numbers.append(int(input()))

# Calculate the first ten digits of the sum
result = first_ten_digits_sum(numbers)

# Output
print(result)
